/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  @font-face {
    font-family: 'Death From Above';
    font-style: normal;
    font-weight: 400;
    font-display: swap;
    src: url('/DeathFromAbove.woff2') format('woff2'),
        url('/DeathFromAbove.woff') format('woff');

  }
}

